# AGENTS.md（Codex 项目规则：增强版 / 可直接用）

> 适用：Vue 3 + JavaScript + ESLint + Element Plus（以项目当前使用的 Element Plus UI 为准）  
> 目标：在“能快速交付”的同时，让代码具备长期维护能力（清晰、稳定、可复用、可扩展）。

---

## TL;DR（前置摘要：即使被截断也要保留）
- **先读后改**：对齐现有项目结构与代码风格；不要自创体系。  
- **Element Plus Plus 优先**：能用 Element Plus Plus 组件就不用自造 UI。  
- **可复用要抽离**：出现 ≥2 次 / 未来可能复用 / 具备独立输入输出 的部分必须抽组件/抽公共逻辑。  
- **最小改动闭环**：不做无关重构；每次改动要可解释、可回滚。  
- **提交前必须过 ESLint**，并跑项目脚本中的 lint / test / build（按项目实际）。  
- **审查看 P0**：错误/回归/竞态/泄漏/安全；其次看重复与一致性。

---

## 1. 工作方式（必须遵守）

### 1.1 做事顺序
1) 读相关代码（同类页面、组件、utils、请求封装、路由/权限、store 等）  
2) 列出改动范围与风险点（高风险先提方案）  
3) 实现最小改动闭环（可复用就抽离）  
4) 自测 + lint/test/build（按项目脚本）  
5) 生成清晰的提交与说明（便于 review 与回滚）

### 1.2 变更边界
- 任务是修 bug → 不做“顺手重构”；若发现系统性问题，先写计划/建议，必要时单独 PR。
- 改公共能力（公共组件/工具/样式）→ 必须评估所有调用方影响并在说明中写清楚。
- 不新增依赖；如新增必须说明：为什么需要、替代方案、影响范围、体积/维护成本。

---

## 2. 技术栈与编码规范

### 2.1 Vue 3 + JavaScript 基本要求
- 遵循项目 ESLint；**提交前不得有 ESLint error**。
- 组件内状态、派生状态、事件处理分层清晰：
  - 状态：data/ref/reactive（依项目写法）
  - 派生：computed
  - 副作用：watch（慎用，优先 computed）
  - 事件：methods / handlers
- 模板表达式保持“短且直观”，复杂逻辑移到 JS 层。
- 任何定时器、事件监听、订阅、轮询：必须在卸载阶段清理（避免泄漏）。

### 2.2 命名与可读性
- 命名要能表达业务含义：`handleSubmit`、`fetchList`、`isDialogVisible`；避免含糊缩写。
- 禁止魔法数字/字符串：抽到常量、枚举、配置（按项目习惯）。
- 复杂逻辑注释写“为什么”，不要写“做了什么”。

### 2.3 错误处理与稳定性
- 所有异步请求必须至少处理：
  - loading 状态（避免重复点击/重复请求）
  - error 提示或兜底（避免静默失败）
  - 空态（空列表/无数据）
  - 竞态（快速切换筛选/分页导致响应乱序覆盖）
- 对外部输入（接口数据、路由参数、用户输入）做空值与类型兜底。

---

## 3. UI 与样式规则（Element Plus 优先）

### 3.1 组件优先级
1) 项目已有公共组件/封装  
2) Element Plus 组件（及其推荐用法）  
3) 自行实现（仅在前两者不可用时）

### 3.2 自行实现 UI 的要求
- 必须保持与 Element Plus 风格一致：间距、字体、交互反馈、禁用态、错误态等。
- 组件必须具备明确的 `props` 输入与 `emits` 输出，避免写死在页面里。
- 需要复用的交互（如通用弹窗、筛选区、表格工具栏、空态）必须抽离。

### 3.3 样式规范
- 优先使用现有全局变量/主题/工具类（如果项目有）。
- 避免全局污染性选择器；在不破坏现有约束的前提下优先 scoped 或模块化方案（以项目为准）。
- 边界必须考虑：长文本、超多行、空数据、滚动容器、窗口缩放、弹窗叠加层。

---

## 4. 组件抽离与复用标准（重点）

### 4.1 必须抽离的判定（满足任一即抽）
- 同类 UI/逻辑在 **两个及以上**页面/模块出现（或可以预见会出现）
- UI 区块具备独立输入输出（props/emits），页面只负责编排
- 文件过长、职责不清，导致维护困难
- 该模块未来可能扩展（字段增加、交互增加、换数据源）且在页面内写死会爆炸

### 4.2 推荐的抽离位置（若项目已有结构，以项目为准）
- `src/components/common/`：可跨业务复用的通用组件
- `src/components/business/`：某类业务通用组件（跨页面复用）
- `src/composables/`：可复用逻辑（弹窗、分页、筛选、请求等）
- `src/utils/`：纯函数工具
- `src/constants/`：常量与枚举
- `src/api/`：接口封装（若项目有统一封装，必须复用）

### 4.3 组件抽离的最低要求
- README/注释说明用途、输入输出、典型用法（简短即可）
- props 有默认值与校验（按项目习惯），避免未定义崩溃
- 对空数据与异常态有兜底
- 不把页面业务强耦合塞进“通用组件”里

---

## 5. 业务代码组织建议（不强制，但优先遵循项目现状）

### 5.1 页面（容器）职责
- 负责：数据获取、状态管理、组合子组件、路由/权限判断、页面级交互编排
- 不负责：重复的 UI 区块、可复用的表单/弹窗/列表单元、通用工具逻辑

### 5.2 组件职责
- 展示与交互为主
- 接口数据加工要尽量放到页面或专门的 formatter/utils 中，避免组件成为“大杂烩”

---

## 6. 提交逻辑（Commit & PR）

### 6.1 提交前检查（必须做）
- ESLint：必须通过（0 error）
- 运行项目脚本（以 package.json 为准）：
  - `lint`
  - 若涉及功能逻辑：`test`（若有）
  - 若影响构建：`build`（若有）
- 删除调试代码、无用日志、无用注释、临时文件

### 6.2 提交粒度
- 一个提交只解决一个主题：不要混合 “功能 + 重构 + 格式化 + 无关优化”
- 大需求拆分：先基础设施/组件抽离，再功能接入，再细节与修补

### 6.3 Commit message（建议 Conventional Commits）
- `feat: ...` 新功能
- `fix: ...` 修复
- `refactor: ...` 重构（不改行为）
- `style: ...` 样式/格式（不改逻辑）
- `chore: ...` 构建/脚本/依赖
- `docs: ...` 文档
要求：能看出改动对象与结果，避免“优化一下”“改点东西”这种描述。

### 6.4 PR 描述模板（建议）
- What：做了什么（要点列表）
- Why：为什么这样做（背景/取舍）
- Impact：影响范围（页面/组件/接口/配置）
- Risk：潜在风险与回滚方式
- Verify：自测方式/脚本输出/截图（若是 UI）

---

## 7. 代码审查标准（Review Checklist：可直接执行）

> 审查输出必须按优先级给出：P0 → P1 → P2，并附“可执行建议”。

### P0 / 必须拦截（不合并）
- 运行错误、白屏、关键流程不可用、数据错误
- 明显回归：破坏公共组件/公共样式/公共工具、破坏既有交互
- 异步竞态导致状态错乱、响应乱序覆盖
- 空值/异常未兜底导致随机崩溃
- 未清理的定时器/监听/订阅导致泄漏或重复触发
- 安全问题：泄露 token/隐私数据、危险的 `v-html/innerHTML` 未做过滤等

### P1 / 必须修（合并前修完）
- 明显重复代码（应抽组件/抽 composable/抽 utils）
- 能用 Element Plus/项目封装却自造 UI，导致一致性差与维护成本上升
- 过深嵌套、过度复杂的条件分支/函数，难以维护与排查
- 命名与结构混乱导致理解成本过高
- 重要边界缺失：空态/加载态/异常态/长文本/极端数据

### P2 / 建议优化（可跟进）
- 代码结构可进一步简化，职责可更清晰
- 样式层级/选择器可更稳健，覆盖性更好
- 可补充少量注释或文档提升交接效率
- 局部性能与可测试性提升建议

---

## 8. Codex 输出要求（你必须这样反馈）
完成任务后，你的最终回复应包含：
- 改动摘要（3~8 条）
- 关键文件列表（含新增/删除）
- 可复用抽离说明（抽了哪些组件/逻辑，放哪）
- 自测与脚本结果（执行了哪些命令）
- 风险点与回滚建议（若有）

---

## 9. 禁止事项
- 不通过 ESLint 直接提交
- 把可能复用的 UI/逻辑写死在页面里
- 无理由新增依赖/大范围改动
- 提交调试日志、临时代码、与需求无关的格式化噪音
